const openBtn = document.querySelector('#open-modal');
const dialog = document.querySelector('#dialog');

openBtn.addEventListener('click', () => dialog.showModal());

// close modal when clicking outside
dialog.addEventListener('click', event => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isInDialog) {
    dialog.close();
  }
});

const open2Btn = document.querySelector('#open-modal2');
const dialog2 = document.querySelector('#dialog2');

open2Btn.addEventListener('click', () => dialog2.showModal());

// close modal when clicking outside
dialog2.addEventListener('click', event => {
  const rect = dialog2.getBoundingClientRect();
  const isIndialog2 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog2) {
    dialog2.close();
  }
});


const open3Btn = document.querySelector('#open-modal3');
const dialog3 = document.querySelector('#dialog3');

open3Btn.addEventListener('click', () => dialog3.showModal());

// close modal when clicking outside
dialog3.addEventListener('click', event => {
  const rect = dialog3.getBoundingClientRect();
  const isIndialog3 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog3) {
    dialog3.close();
  }
});

const open4Btn = document.querySelector('#open-modal4');
const dialog4 = document.querySelector('#dialog4');

open4Btn.addEventListener('click', () => dialog4.showModal());

// close modal when clicking outside
dialog4.addEventListener('click', event => {
  const rect = dialog4.getBoundingClientRect();
  const isIndialog4 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog4) {
    dialog4.close();
  }
});

const open5Btn = document.querySelector('#open-modal5');
const dialog5 = document.querySelector('#dialog5');

open5Btn.addEventListener('click', () => dialog5.showModal());

// close modal when clicking outside
dialog5.addEventListener('click', event => {
  const rect = dialog5.getBoundingClientRect();
  const isIndialog5 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog5) {
    dialog5.close();
  }
});

const open6Btn = document.querySelector('#open-modal6');
const dialog6 = document.querySelector('#dialog6');

open6Btn.addEventListener('click', () => dialog6.showModal());

// close modal when clicking outside
dialog6.addEventListener('click', event => {
  const rect = dialog6.getBoundingClientRect();
  const isIndialog6 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog6) {
    dialog6.close();
  }
});

const open7Btn = document.querySelector('#open-modal7');
const dialog7 = document.querySelector('#dialog7');

open7Btn.addEventListener('click', () => dialog7.showModal());

// close modal when clicking outside
dialog7.addEventListener('click', event => {
  const rect = dialog7.getBoundingClientRect();
  const isIndialog7 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog7) {
    dialog7.close();
  }
});

const open8Btn = document.querySelector('#open-modal8');
const dialog8 = document.querySelector('#dialog8');

open8Btn.addEventListener('click', () => dialog8.showModal());

// close modal when clicking outside
dialog8.addEventListener('click', event => {
  const rect = dialog8.getBoundingClientRect();
  const isIndialog8 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog8) {
    dialog8.close();
  }
});

const open9Btn = document.querySelector('#open-modal9');
const dialog9 = document.querySelector('#dialog9');

open9Btn.addEventListener('click', () => dialog9.showModal());

// close modal when clicking outside
dialog9.addEventListener('click', event => {
  const rect = dialog9.getBoundingClientRect();
  const isIndialog9 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog9) {
    dialog9.close();
  }
});

const open10Btn = document.querySelector('#open-modal10');
const dialog10 = document.querySelector('#dialog10');

open10Btn.addEventListener('click', () => dialog10.showModal());

// close modal when clicking outside
dialog10.addEventListener('click', event => {
  const rect = dialog10.getBoundingClientRect();
  const isIndialog10 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog10) {
    dialog10.close();
  }
});

const open11Btn = document.querySelector('#open-modal11');
const dialog11 = document.querySelector('#dialog11');

open11Btn.addEventListener('click', () => dialog11.showModal());

// close modal when clicking outside
dialog11.addEventListener('click', event => {
  const rect = dialog11.getBoundingClientRect();
  const isIndialog11 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog11) {
    dialog11.close();
  }
});

const open12Btn = document.querySelector('#open-modal12');
const dialog12 = document.querySelector('#dialog12');

open12Btn.addEventListener('click', () => dialog12.showModal());

// close modal when clicking outside
dialog12.addEventListener('click', event => {
  const rect = dialog12.getBoundingClientRect();
  const isIndialog12 =
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom;

  if (!isIndialog12) {
    dialog12.close();
  }
});